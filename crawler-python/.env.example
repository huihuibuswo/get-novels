# ==========================================
# 代理配置说明
# ==========================================

# 允许访问后端的前端域名，多个域名用逗号分隔
# Render 部署时建议设为实际前端地址，例如：
# CORS_ORIGINS=https://your-frontend.onrender.com

# 可选：兼容 RSS 的搜索服务地址，必须保留 {query} 占位符
# NOVEL_SEARCH_RSS_URL=https://www.bing.com/search?format=rss&q={query}

# 可选：DuckDuckGo 备用搜索入口，必须保留 {query} 占位符
# DUCKDUCKGO_HTML_URL=https://lite.duckduckgo.com/lite/?q={query}

# 方式一：固定代理（推荐本地运行有 Clash、V2Ray 等代理工具时使用）
# 格式：http://IP:端口 或 http://用户名:密码@IP:端口
# 例如本地 Clash 默认端口通常为 7890，可以配置为：
# OUTBOUND_PROXY=http://127.0.0.1:7890

# 方式二：动态代理 API（每次请求该 URL 会返回一行新的 IP:PORT，如“112.112.112.112:8888”）
# PROXY_API_URL=
# 代理轮换频率：每发送几次请求更换一次 IP（0 为不主动更换）
# PROXY_ROTATION_INTERVAL=1
